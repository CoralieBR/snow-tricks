<div class="comment">
    <div>{{ comment.createdAt|date('d/m/Y H:i') }}</div>
    <div>
        <div>
            <div>🏂</div>
            <div>{{ comment.user.name }}</div>
        </div>
        <div >
            <p>{{ comment.content }}</p>
        </div>
    </div>
</div>