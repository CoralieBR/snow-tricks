{% extends 'base.html.twig' %}

{% block title %}Trick: {{ trick.name }}{% endblock %}

{% block body %}
<main class="trick-page">
    <div id="trickpage-picture-container" >
        <img src="{{ placeholderPicture }}" alt="photo de montagne enneigée">
    </div>

    <turbo-frame id="triiick-{{ trick.id }}" class="current-frame">
        <div id="modal-{{ trick.id }}" class="trick-modal">
            <div id="trick-container">
                {% if isModal %}
                    <a style="color: white"href="{{ path('app_homepage') }}">FERMER ⊗</a>
                {% endif %}
                <div>
                    <img src="{{ placeholderPicture }}" alt="photo de montagne enneigée">
                </div>
                <div class="other-trick-pictures">
                    {% for  i in 0..6 %}
                        <div>
                            <img src="{{ placeholderPicture }}" alt="photo de montagne enneigée">
                            {% if edit|default(false) %}
                                <div>
                                    <button>✏️</button>
                                    <button>🗑️</button>
                                </div>
                            {% endif %}
                        </div>
                    {% endfor %}
                </div>
                <div id="trick-infos">
                    <h1>{{ trick.name }}</h1>
                    {% for group in trick.groups %}
                    <p><i>{{ group.name }}</i></p>
                    {% endfor %}
                    <p>{{ trick.description }}</p>
                    
                </div>
            </div>
        </div>
    </turbo-frame>
</main>
{% endblock %}
