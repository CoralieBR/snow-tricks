<turbo-stream action="append" target="other-trick-pictures">
    <template>
        {% for medium in media %}
            <div class="medium-container">
                {% if medium.type == 'picture' %}
                    <img src="{{ asset('uploads/media/' ~ medium.path) }}" alt="photo de montagne enneigée">
                {% elseif medium.type == 'video' %}
                    <iframe max-width="100vw" src="{{ medium.path }}" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe>
                {% endif %}
            </div>
        {% endfor %}
    </template>
</turbo-stream>

<turbo-stream action="remove" target="see-more">
</turbo-stream>